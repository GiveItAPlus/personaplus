lintScript: "lint"
prettyScript: "prettify"
destroy:
    intensities: "*"
    targets:
        - ".expo/"
updateCmdOverride: "upd"
flagless:
    flaglessLint: true
    flaglessPretty: true
    flaglessDestroy: true
    flaglessUpdate: true
launchCmd:
    - $"start:dev"
commitCmd:
    # - $"project:check"
    - $"upd"
    - $"lint"
    - $"prettify"
